import React from 'react';
import Typography from '@material-ui/core/Typography';

const SavedReportsScreen = () => {
  return (
    <div>
      <Typography variant="h1">Saved Reports Screen</Typography>
    </div>
  );
};

export default SavedReportsScreen;