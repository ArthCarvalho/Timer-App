import React from 'react';
import Typography from '@material-ui/core/Typography';

const TeamScreen = () => {
  return (
    <div>
      <Typography variant="h1">Team Screen</Typography>
    </div>
  );
};

export default TeamScreen;