import React from 'react';
import Typography from '@material-ui/core/Typography';

const WorkspacesScreen = () => {
  return (
    <div>
      <Typography variant="h1">Workspaces Screen</Typography>
    </div>
  );
};

export default WorkspacesScreen;