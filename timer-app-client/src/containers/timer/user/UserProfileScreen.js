import React from 'react';
import Typography from '@material-ui/core/Typography';

const UserProfileScreen = () => {
  return (
    <div>
      <Typography variant="h1">User Profile Screen</Typography>
    </div>
  );
};

export default UserProfileScreen;