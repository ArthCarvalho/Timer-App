import React from 'react';
import Typography from '@material-ui/core/Typography';

const UserSettingsScreen = () => {
  return (
    <div>
      <Typography variant="h1">User Settings Screen</Typography>
    </div>
  );
};

export default UserSettingsScreen;