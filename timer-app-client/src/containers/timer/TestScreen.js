import React from 'react';
import Typography from '@material-ui/core/Typography';

const TestScreen = () => {
  return (
    <div>
      <Typography variant="h1">Test Screen</Typography>
    </div>
  );
};

export default TestScreen;