import React from 'react';
import Typography from '@material-ui/core/Typography';

const ClientsScreen = () => {
  return (
    <div>
      <Typography variant="h1">Clients Screen</Typography>
    </div>
  );
};

export default ClientsScreen;